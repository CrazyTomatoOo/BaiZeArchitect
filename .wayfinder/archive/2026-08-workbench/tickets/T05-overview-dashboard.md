# T05 — 总览仪表盘内容 `wayfinder:prototype`

status: closed
assignee: pi(UI built)
blocked-by: T02

## Question

总览仪表盘展示什么(跨 workspace 汇总):

- 计数卡:需求数/场景数/用例数/功能项数/待决策数。
- 各 workspace 健康度(需求阶段分布、待决策堆积)。
- 最近活动(最近 run/最近沉淀资产)。
- 与现有"复用仪表盘"(evidence/ADR/gene)是合并还是并列?

产出:原型页 + gateway /api/overview 聚合端点。
