# macOS AppleDouble 资源叉文件(在 /Volumes 外置盘上 xattr 会炸 build context)
._*
**/._*
.DS_Store
**/.DS_Store

# 镜像内自己 npm install,不打入宿主 node_modules
**/node_modules

# 旧三服务已归档,不进镜像
_archived
backups

# 运行时数据/挂载目录(不进镜像,避免 ._ 炸 context + 加速 build)
# lws/test-repo-2 由 run.sh -v 挂载;evidence/evolver-home/out 是运行时数据
test-repo-2
lws
evolver-home
evidence
out
.git

# 无关目录
docs
.github
.omo
.codegraph
.playwright-mcp
examples
scripts
*.md
!schemas/**
